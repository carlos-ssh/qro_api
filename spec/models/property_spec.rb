require 'rails_helper'

RSpec.describe Property, type: :model do
  # describe '#validations' do
  #   it 'should test that the factory is valid' do
  #     expect build(:property).to be_valid
  #   end

  #   it 'should validate the presence of the name' do
  #     property = build :property, name: ''
  #     expect(property).to_not be_valid
  #     expect(property.errors.messages[:name]).to include("can't be blank")
  #   end

  #   it 'should validate the presence of the address' do
  #     property = build :property, address: ''
  #     expect(property).to_not be_valid
  #     expect(property.errors.messages[:address]).to include("can't be blank")
  #   end

    # it 'should validate the presence of the description' do
    #   property = build :property, description: ''
    #   expect(property).to_not be_valid
    #   expect(property.errors.messages[:description]).to include("can't be blank")
    # end

    # it 'should validate the presence of the price' do
    #   property = build :property, price: nill
    #   expect(property).to_not be_valid
    #   expect(property.errors.messages[:price]).to include("can't be blank")
    # end

    # it 'should validate the presence of the rooms' do
    #   property = build :property, rooms: nill
    #   expect(property).to_not be_valid
    #   expect(property.errors.messages[:rooms]).to include("can't be blank")
    # end

    # it 'should validate the presence of the bathrooms' do
    #   property = build :property, bathrooms: nill
    #   expect(property).to_not be_valid
    #   expect(property.errors.messages[:bathrooms]).to include("can't be blank")
    # end

    # it 'should validate the presence of the meters' do
    #   property = build :property, meters: nil
    #   expect(property).to_not be_valid
    #   expect(property.errors.messages[:meters]).to include("can't be blank")
    # end

    # it 'should validate the presence of the garage' do
    #   property = FactoryBot.build :property, garage: null
    #   expect(property).to_not be_valid
    #   expect(article.errors.messages[:garage]).to include("can't be blank")
    # end

    # it 'should validate the presence of the image' do
    #   property = FactoryBot.build :property, image: ''
    #   expect(property).to_not be_valid
    #   expect(article.errors.messages[:image]).to include("can't be blank")
    # end

    # it 'should validate the presence of the for_sale' do
    #   property = FactoryBot.build :property, for_sale: null
    #   expect(property).to_not be_valid
    #   expect(article.errors.messages[:for_sale]).to include(true)
    # end

    # it 'should validate the presence of the status' do
    #   property = FactoryBot.build :property, status: ''
    #   expect(property).to_not be_valid
    #   expect(article.errors.messages[:status]).to include("can't be blank")
    # end

    # it 'should validate the presence of the available_date' do
    #   property = FactoryBot.build :property, available_date: ''
    #   expect(property).to_not be_valid
    #   expect(article.errors.messages[:available_date]).to include("can't be blank")
    # end
  # end
end
